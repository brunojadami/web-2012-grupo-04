package webproject.validation;

/**
 * Validador acadêmico.
 */
public class AcademicValidator extends Validator
{
    /**
     * @param level Nível acadêmico.
     * @param errorMessage Mensagem de error.
     * @return Nulo se sucesso, a mensagem de erro caso erro.
     */
    public String validateAcademicLevel(String level, String errorMessage)
    {
        return null;
    }
    
    /**
     * @param status Estado acadêmico.
     * @param errorMessage Mensagem de erro.
     * @return Nulo se sucesso, a mensagem de erro caso erro.
     */
    public String validateAcademicStatus(String status, String errorMessage)
    {
        return null;
    }
}
